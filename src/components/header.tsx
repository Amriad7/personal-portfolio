import NavMenu from "./ui/nav-menu";

const Header = () => {
  return (
    <header className="flex items-center justify-center h-[80px] w-full fixed">
      <NavMenu />
    </header>
  );
};
export default Header;
